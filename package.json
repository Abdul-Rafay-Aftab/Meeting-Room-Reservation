{
  "name": "meeting-room-reservation-system",
  "version": "1.0.0",
  "description": "A complete web-based meeting room reservation system with user authentication and admin panel",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup-db": "cd server && npm run setup-db"
  },
  "keywords": ["meeting", "reservation", "room", "booking", "admin"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 